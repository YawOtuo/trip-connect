import FramerWrapper from "@/components/FramerWrapper";
import { fadeIn } from "@/lib/animations";

function TellUsMore() {
  return <FramerWrapper {...fadeIn}>TellUSmore</FramerWrapper>;
}

export default TellUsMore;
