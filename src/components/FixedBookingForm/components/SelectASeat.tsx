import FramerWrapper from "@/components/FramerWrapper";
import { fadeIn } from "@/lib/animations";

function SelectASeat() {
  return <FramerWrapper {...fadeIn}>Select a seat (
    
    )</FramerWrapper>;
}

export default SelectASeat;
